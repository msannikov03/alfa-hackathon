services:
  backend:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
    volumes: []
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      target: production
    command: npm start
    volumes: []
    restart: unless-stopped
